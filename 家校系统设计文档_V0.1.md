# 家校系统设计方案（V0.1，待评审）

> 目标：家长在手机上收到自己孩子的成绩和学习指导建议。
> 本文档只讨论设计，不含代码。**状态：草案，等你确认方向后再动手。**

---

## 1. 先说最关键的一个问题

这件事的难点**不在选什么手机框架**，而在于：

```
现在：后端跑在你自己的电脑上，监听 127.0.0.1:8000
      关掉 start.bat 就没了，家长的手机根本访问不到
```

家长要在手机上看到成绩，**数据必须离开你的电脑**，去到一个家长能访问的地方。
怎么离开、离开多少、放在哪，决定了整个方案的复杂度和成本。这一步想清楚了，
前端用什么框架反而是最容易的部分。

围绕这一点有三条路，复杂度差好几个数量级。

---

## 2. 三个方案

### 方案A：导出报告，用微信发给家长（零基础设施）

批改完成后，为每个学生生成一张**成绩报告图片**（或 PDF），教师在微信里发给家长。

```
教师电脑（现有系统）→ 生成图片 → 教师手动用微信发送 → 家长
```

- **要开发的**：报告图片生成 + 批量导出（约 2~3 天工作量）
- **要花的钱**：0
- **家长要做的**：什么都不用做，在微信里点开图片即可
- **缺点**：教师要手动发 N 次（可按班级打包，但仍需人工）；家长看不到历史成绩

> 这不是"凑合方案"。很多老师现在就是这么做的，它的优势是**今天就能用**，
> 不需要服务器、不需要备案、不需要家长注册、没有任何数据合规风险。

### 方案B：专属链接 + 网页报告（轻量）

每个学生生成一个**不可猜测的专属链接**，教师把链接发给对应家长，
家长用手机浏览器或微信打开就能看，包括历史成绩。

```
教师电脑 → 只上传"已生成的报告" → 云端静态托管 → 家长点链接看
```

- **要开发的**：报告页面 + 上传发布功能 + 链接管理（约 1~2 周）
- **要花的钱**：静态托管每月几元到十几元
- **家长要做的**：点链接，可收藏
- **缺点**：没有推送通知；链接一旦转发给别人就能看（需靠链接足够长+可失效来缓解）
- **注意**：在国内用域名做公开网站涉及备案问题，需要你确认当前政策要求

### 方案C：微信小程序（完整）

家长在微信里搜索/扫码进入小程序，绑定孩子后可查看历次成绩、错题解析，
新成绩发布时收到微信推送。

```
教师电脑 → 同步数据 → 云端后端 → 微信小程序 → 家长
```

- **要开发的**：小程序端 + 云端后端 + 绑定关系 + 推送（约 3~5 周）
- **要花的钱**：云开发按量计费，小规模每月几元~几十元
- **家长要做的**：微信里打开，首次绑定孩子
- **缺点**：需要注册小程序账号并通过审核；**教育类目对主体资质可能有要求**
  （个人主体能否注册教育类小程序、是否需要学校授权，这一点**需要你去微信公众平台确认**，
  我不确定当前的具体规定，不敢替你打包票）

---

## 3. 我的建议：按 A → B → C 分阶段

不建议直接上来做小程序，原因有三：

1. **需求还没被验证。** 家长到底会不会用、想看什么，现在只是猜测。
   方案A 花两三天就能试出来，如果家长反馈"就想看个分数"，那方案C 的大部分功能都是白做的。
2. **方案A 的产出是后面两个方案的基础。** 报告的内容和排版做好了，
   B 和 C 都是换个壳展示同样的东西，不浪费。
3. **小程序的门槛不在技术，在账号和资质。** 万一卡在审核上，前面写的后端就都悬着了。
   先做 A，边做边去把账号的事问清楚。

---

## 4. 技术选型

### 如果走方案A（报告图片）

在现有后端里加，不引入新技术栈：

| 方案 | 说明 | 取舍 |
|---|---|---|
| **HTML 模板 → 截图**（推荐） | 用 Jinja2 写报告页，Playwright 无头浏览器截成图 | 排版用 CSS，改样式容易；要多装一个浏览器内核（约 100MB） |
| Pillow 直接画 | 纯 Python 画图 | 无额外依赖；但中文排版、换行、表格全要手写坐标，改一次样式很痛苦 |
| 生成 PDF | reportlab / weasyprint | 适合打印存档；但手机上看 PDF 体验不如图片 |

**建议 HTML 模板 + 截图**：报告要放分数表、雷达图、文字建议，用 CSS 排版比手写坐标省事太多，
而且这套 HTML 以后在方案B 里可以直接当网页用。

### 如果走方案C（小程序）

| 选项 | 说明 | 适不适合你 |
|---|---|---|
| **微信原生小程序**（推荐） | WXML/WXSS/JS，官方工具链 | 家长端就是"看报告"，功能简单，原生够用且最省事，文档最全 |
| uni-app | Vue 语法，一套代码编译到小程序/H5/App | 只做微信的话，多一层编译反而增加调试难度 |
| Taro | React 语法，你写过本项目前端会熟悉 | 同上；若将来确定要多端，再考虑 |

**云端后端**：建议用**微信云开发（CloudBase）**而不是自己买服务器——
它是 serverless，不用管运维，和小程序登录天然打通（能直接拿到用户唯一标识做绑定），
省掉自己写账号系统。规模小的时候费用很低。

> 注意：微信云开发只能给小程序用。如果以后还想要网页版，就得换成普通云服务器。

---

## 5. 数据怎么从你的电脑出去

这是方案B/C 都要解决的问题。**原则：只上传必要的结果，不上传原始试卷。**

```
留在你电脑上（永不上传）        上传到云端
─────────────────────      ─────────────────
试卷扫描件原图                   学生姓名（或化名）
参考答案                        总分、各题得分
AI 的完整批改过程                错题的题号与简要解析
教师的 API Key                  学习建议文字
班级完整名单                     考试名称、日期
```

扫描件是最敏感的（有笔迹、有姓名、可能有其它个人信息），**没有任何理由上传**。
家长要看的是结论，不是原始卷子。

同步方式：教师在系统里点"发布成绩"，才把这一次考试的结果推上去；不点就不出门。
**教师始终掌握"发不发"的开关**，这点很重要——有些成绩老师可能不想公开。

---

## 6. 隐私和风险（这部分请你重点看）

学生成绩是**未成年人的个人信息**，比普通数据敏感得多。

1. **家长只能看到自己孩子的。** 绑定关系必须校验，不能出现改个 ID 就看到别人成绩的漏洞。
2. **不做排名展示。** 给家长看"全班第3名"会带来压力和攀比，也放大了泄露的危害。
   建议只给本人成绩 + 班级平均分（不给具体名次）。
3. **链接必须不可猜测。** 方案B 的链接要用足够长的随机串，且能随时作废。
4. **要有知情同意。** 在推给家长之前，最好先跟学校/家长说明会同步哪些内容。
5. **AI 的批改会出错。** 我们已经实测过，同一份试卷不同批次的分数会有几分波动。
   所以**推给家长之前必须经过教师确认**，不能批改完自动发送——
   一旦家长看到的分数和老师最终认定的不一致，很难解释。

第 5 点建议做成硬性流程：**成绩必须教师点过"确认发布"才对家长可见。**

---

## 7. 家长端界面草案（方案A 的报告内容）

```
┌─────────────────────────┐
│  张三  五年级期末语文       │
│  2026-09-21              │
├─────────────────────────┤
│        94 / 100          │
│   班级平均 85.3          │
├─────────────────────────┤
│  积累运用   34/34        │
│  阅读感悟   32/36        │
│  作文       28/30        │
├─────────────────────────┤
│  本次错题                 │
│  · 阅读(二)2  2/3        │
│    未答出"对比"这一手法    │
│  · ……                   │
├─────────────────────────┤
│  给孩子的建议              │
│  1. ……                  │
│  2. ……                  │
└─────────────────────────┘
```

内容来源：现有的批改结果里已经全有了（逐题得分、扣分原因、作文建议），
班级平均分来自成绩统计。**不需要重新调用 AI**，直接组装即可——这也是方案A 便宜的原因。

> 「给孩子的建议」这部分，现有数据里只有作文的建议是现成的。
> 如果要针对整份试卷给个性化学习指导，需要每个学生再调一次 AI（约 0.02 元/人）。
> 这正好和你之前排的"错题解析与学习指导"那个中优先级需求重合，可以合并做。

---

## 8. 需要你决定的事

1. **先做哪个方案？** 我建议 A，但如果你已经确定要做小程序，也可以直接上 C。
2. **家长端要不要看历史成绩？** 只看本次 → 方案A 够用；要看历次趋势 → 必须 B 或 C。
3. **要不要推送通知？** 要 → 只能方案C；不要 → A/B 都行。
4. **给家长的"学习建议"是每人单独生成，还是共用班级共性建议？**
   单独生成效果好但每人多花约 0.02 元；共用的话零成本但针对性弱。
5. **小程序账号的事**：如果倾向方案C，请先去微信公众平台确认你的主体能否注册、
   教育类目有什么要求。这一步卡住的话方案C 就走不通，值得先问。

---

## 9. 如果选方案A，下一步

1. 设计报告模板（我出 HTML 草稿，你看排版）
2. 在批改结果页加「生成家长报告」按钮，单人预览
3. 成绩统计页加「批量导出本班报告」，导出成一个文件夹，教师自行发送
4. 再决定要不要为每个学生单独生成学习建议
