# 语文试卷AI批改系统 —— 项目技术方案（V0.1，待评审）

> 本文档是**技术方案 / 项目规划文档**，用于确定技术选型、系统架构和开发路线图。
> 具体的批改逻辑、评分规则、Prompt 原则已经在 [语文试卷AI批改工作流与评分规范_V1.0.md](./语文试卷AI批改工作流与评分规范_V1.0.md) 中详细定义，本文档不重复其内容，只说明"用什么技术、怎么把这套规则落地成一个可运行的系统"。
>
> 状态：**草案，等待确认后再开始编码。**

---

## 1. 项目定位

这是一个**面向教师个人/单个班级使用的语文试卷AI批改工具**，核心价值是：

- 教师上传参考答案 + 学生试卷扫描件
- 程序按照既定工作流，调用教师自己配置的多模态大模型 API 完成批改
- 程序本身**不训练模型、不做本地OCR/CV算法**，核心壁垒是"工作流编排 + Prompt 设计 + 结构化数据管理"
- AI 模型能力（识别手写、理解语义、评分）完全依赖外部 API（DeepSeek、豆包、千问等多模态模型），教师自备 API Key

这个定位决定了技术选型的核心原则：**后端做"编排 + 存储 + 调用"，不做"重计算"**，所以不需要为了跑模型而选择支持GPU的复杂技术栈。

---

## 2. 技术选型

### 2.1 总体方案

| 层 | 选型 | 理由 |
|---|---|---|
| 后端语言/框架 | **Python + FastAPI** | Python 生态对"调用大模型API、处理图片、拼JSON、写Prompt"最friendly；FastAPI 原生异步，适合"多份试卷并发调用API"这种 I/O 密集场景；自带 OpenAPI 文档，方便你自己调试接口 |
| 数据库 | **SQLite（起步）→ 预留迁移到 PostgreSQL** | 单机/单教师场景下 SQLite 零运维、文件即备份；用 SQLAlchemy ORM 编写，后续数据量变大或要支持家校系统的多端读写时，换 PostgreSQL 只需改连接串，不用重写业务代码 |
| 图片/文件存储 | 本地磁盘目录（按 `试卷批次/学生/题目` 分层） | 现阶段没有多机部署需求，本地存储最简单；预留一个 `StorageBackend` 接口，未来要上云（阿里云OSS/腾讯云COS）只需换实现 |
| 前端（教师端网页） | **React + TypeScript + Vite + Ant Design** | 教师端本质是一个"上传—查看进度—看报告"的后台管理系统，Ant Design 的表格、上传组件、表单校验都是现成的，能显著减少你自己写UI的工作量；React 生态资料多，后续想加功能容易找到参考 |
| 与AI模型通信 | 统一走 **OpenAI 兼容的 Chat Completions 协议**（DeepSeek / 豆包 / 千问的多模态接口都兼容或接近这个协议） | 只写一套"模型调用客户端"，通过配置切换 `base_url` / `model` / `api_key` 即可换模型，不用为每家单独写代码 |
| 部署方式 | 单机 Docker（后端+前端+SQLite 打包一个 compose） | 教师自己一台电脑/一台小型云主机就能跑起来，不依赖复杂运维 |

### 2.2 为什么不选其它方案（简要说明，供你确认）

- **不用 Node.js/Nest.js 做后端**：图片预处理、拼装多模态 Prompt、和各家模型 SDK 对接，Python 的资料和示例远多于 Node，出问题更容易查。
- **不上 Celery/Redis 这类任务队列（暂时）**：批量批改是"教师点一次、等结果"的场景，用 FastAPI 自带的后台任务 + 简单的并发限流（比如同时最多5个API请求）就够用；等未来单次批改量大到需要跨进程/跨机器扩展时，再引入队列，现在引入是过度设计。
- **不用 Electron 做桌面客户端**：网页版可以直接在浏览器用，跨平台、免安装，也方便未来老师在学校电脑、家里电脑都能用同一套账号数据。

---

## 3. 系统架构

```text
┌─────────────────────────┐
│   教师端网页 (React)     │
│  - 上传参考答案/试卷      │
│  - 配置 API Key/模型      │
│  - 查看批改进度与报告      │
└────────────┬─────────────┘
             │ HTTP/JSON
┌────────────▼─────────────┐
│      后端 API (FastAPI)   │
│                           │
│  ┌─────────────────────┐ │
│  │  批改任务编排器        │ │   ← 严格按照工作流文档的12个步骤驱动
│  │ (Workflow Orchestrator)│ │
│  └──────────┬──────────┘ │
│             │             │
│  ┌──────────▼──────────┐ │
│  │  Prompt 模板管理      │ │   ← 对应文档第31/32章的评分Prompt原则
│  └──────────┬──────────┘ │
│             │             │
│  ┌──────────▼──────────┐ │
│  │  模型调用适配层        │ │   ← 统一接口，内部适配 DeepSeek/豆包/千问
│  │ (Model Provider)      │ │
│  └──────────┬──────────┘ │
│             │             │
│  ┌──────────▼──────────┐ │
│  │  数据存储层            │ │   ← SQLite + 本地文件
│  │ (试卷/题目/成绩/学生)  │ │
│  └──────────────────────┘ │
└───────────────────────────┘
             │
             ▼
   教师自己的模型 API Key
   (DeepSeek-VL / 豆包 / 千问-VL)
```

后端内部按照工作流文档的流程拆成几个独立模块，方便以后单独修改某一环节而不影响其它部分：

1. **ReferenceParser**：把参考答案（Word/图片/文本）解析成结构化 JSON（对应文档第5章）
2. **PaperSegmenter**：把学生扫描件切成逐题图片（对应文档第7–8章）
3. **AnswerRecognizer**：调用多模态模型识别每题学生作答（对应文档第9章）
4. **ObjectiveGrader**：规则引擎，处理选择题/判断题/精确匹配题（对应文档第10章）
5. **SubjectiveGrader**：调用大模型按"得分点覆盖"评分（对应文档第13–19章）
6. **EssayGrader**：作文独立评分模块（对应文档第20–23章）
7. **ConfidenceReviewer**：置信度判断与二次复核（对应文档第24–25章）
8. **ReportGenerator**：生成学生报告与班级统计（对应文档第26–28章）

这样划分的好处：**每个模块对应文档里的一章**，以后你想调整某类题的评分方式，只改一个模块的 Prompt/逻辑，不会牵动整个系统。

---

## 4. 核心数据模型（草案）

```text
Exam           试卷模板：名称、总分、各大题结构（对应文档第5章的JSON）
Question       题目：所属试卷、题型、分值、评分点/参考答案
ClassGroup     班级（P1功能）：班级名称、学生名单
Student        学生（P1功能）：姓名、所属班级、家长联系方式（预留，P3功能用）
Submission     一份学生试卷：关联 Exam、关联 Student（可为空，未匹配学生前）、原始扫描件、批改状态
AnswerRecord   单题作答记录：关联 Submission+Question、识别文本、置信度、
               得分/缺失/错误得分点、最终得分、复核标记（对应文档第26/30章结构）
EssayRecord    作文记录：全文转写、字数、各维度评分、优缺点、建议（对应文档第23章结构）
ModelConfig    教师配置的API信息：厂商、base_url、model名称、api_key（加密存储）
```

这套模型天然支持"同时批改多份试卷"（多个 Submission 并行处理）和后续的班级统计（按 ClassGroup 聚合 Submission）。

---

## 5. API Key 配置界面（较高优先级功能）

按你的要求做成"教师只需要把 API Key 粘贴进框里就能用"：

- 界面上提供一个下拉选择：**DeepSeek / 豆包 / 千问 / 自定义（OpenAI兼容）**
- 选定厂商后，`base_url` 和默认模型名自动填好，教师只需粘贴 Key
- 提供"测试连接"按钮：发一次最小请求验证 Key 有效、模型支持多模态
- Key 在数据库中加密存储，不在前端日志/网络请求中明文暴露给第三方

---

## 6. 分阶段路线图

> 严格按你给的优先级排列，每个阶段都是可独立交付、可运行的版本，不要求一次性做完。

### 第一阶段（MVP，最高优先级）—— 能跑通核心批改流程

- 参考答案解析（先支持 Word/文本，图片版参考答案可后置）
- 学生试卷批量上传（一次多份），每份试卷独立记录、独立可查看结果
- 按工作流文档完整跑通：切题 → 识别 → 客观题规则评分 → 主观题/作文AI评分 → 置信度标记
- API Key 配置界面（DeepSeek/豆包/千问 + 自定义）
- 批改结果页面：每题得分、扣分原因、需人工复核的题目高亮

**验收标准**：教师上传一份参考答案 + 5份学生扫描件，能在页面上看到5份独立的批改结果，每处扣分都能看到理由。

### 第二阶段（中优先级）—— 班级与学情

- 班级系统：创建班级、导入学生名单
- 教师完成"试卷—学生"匹配（人工点选或按扫描顺序+姓名识别辅助匹配）后，成绩计入该学生的历史记录
- 学生成绩趋势（多次考试对比，可先做最简单的列表/折线图）

### 第三阶段（中优先级）—— 错题解析与学习指导

- 基于 AnswerRecord 里的"缺失得分点/错误得分点"，调用大模型生成针对该学生的错题解析和学习建议
- 支持导出/打印单个学生的"批改报告+学习建议"

### 第四阶段（最低优先级）—— 家校同步

- **建议采用微信小程序，而不是独立App**，理由：
  - 家长不需要额外安装App，扫码/搜索即用，推广阻力最小
  - 微信小程序在国内家校场景是事实标准（大量学校家校通知类应用都是小程序）
  - 后端已经是标准REST API，小程序只是多一个前端消费方，改动量小
  - 如果未来确实需要更复杂的家长端能力（推送提醒、离线查看），再评估是否值得做独立App
- 功能范围（后置细化）：家长绑定学生、查看成绩与错题解析、成绩变化提醒

---

## 7. 需要你确认/决策的问题

1. **模型确认**：DeepSeek、豆包、千问的具体模型名称/接口版本（例如 `deepseek-vl2` 还是其它），需要你提供你实际会使用的模型型号，以便我在"模型适配层"里预置正确的调用参数。
2. **参考答案的常见格式**：目前看到的是 `.docx`，是否所有参考答案都是这种排版风格？评分点/分值是否总是明确写出，还是有时需要老师补充配置（对应文档第10.3/11章提到的"需要评分参数"情况）？
3. **试卷—学生匹配方式**：第二阶段的"姓名与试卷对应"，是完全人工在界面上一份份指定，还是希望AI辅助识别试卷上的姓名栏来自动匹配（后者需要额外的识别环节）？
4. **部署环境**：这个系统最终会跑在你自己的电脑上，还是要放到云服务器让多个老师使用？这会影响是否需要账号体系/多用户隔离（目前方案默认按"单教师使用"设计，多教师需要加一层账号权限）。

---

## 8. 下一步

等你确认以上技术方案和第7节的问题后，我会先落地**第一阶段（MVP）**：项目脚手架 + 参考答案解析 + 批改工作流编排 + 模型调用层 + 最基础的上传/查看界面，先跑通"上传—批改—看结果"这条主线，再逐步叠加班级系统、错题解析、家校同步。
